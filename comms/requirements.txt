# Kaart Comms service — runtime dependencies.
#
# Self-contained Flask app with its own database, deployed as its own App
# Platform component. Versions are pinned to match Mikro's
# backend/requirements.txt where the same package appears, so the shared
# repo resolves one consistent set of versions.

# Core Flask
flask>=3.0.0
flask-sqlalchemy>=3.1.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Auth0 JWT validation
python-jose[cryptography]>=3.3.0

# HTTP requests (JWKS fetch, etc.)
requests>=2.31.0

# Server
gunicorn>=21.0.0
