# Core Flask
flask>=3.0.0
flask-cors>=4.0.0
flask-migrate>=4.0.0
flask-sqlalchemy>=3.1.0
flask-mail>=0.9.1
werkzeug>=3.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Auth0 JWT validation
python-jose[cryptography]>=3.3.0

# HTTP requests
requests>=2.31.0

# MapRoulette API wrapper
maproulette>=1.8.1

# Environment
python-dotenv>=1.0.0

# Server
gunicorn>=21.0.0

# PDF export + ASCII transliteration for non-Latin
# OSM usernames so report exports are readable.
reportlab>=4.0.0
Unidecode>=1.3.0

# Utilities
marshmallow>=3.20.0
marshmallow-sqlalchemy>=0.29.0

# Anthropic API (AI channel summaries)
anthropic>=0.40.0

# AWS (if needed for file storage)
boto3>=1.34.0

# Transcription (server-side Whisper)
faster-whisper>=1.0.0

# Dev tools
black>=24.0.0
flake8>=7.0.0
autoflake>=2.3.0
vulture>=2.16
libcst>=1.8.0
